status: 回填節奏定案、schema v11 裁示紀錄，並提出 6.3 拆分提案

§5  「待決 — 回填與抽取的節奏」改列為已決（人工裁示 2026-08-09，方案 1 ＋ 2）：
      - §7 開抽之前先跑一批 5.5 回填（標記效能，18 列，high 密度最高）
      - 其後每抽一節配兩批回填
      - 回填批次以「同一張表／同一組數值」為單位取列，不以 priority 為單位。
        此條在同一 section 內優先於 runbooks/backfill.md §2 的「依 priority
        降冪處理」——節點內先做完成組的那一組，剩下的再依 priority。
        依據是 queries.md §1.15：4.1.3 兩批證明成組查會撿到分散查撿不到的
        東西，第二批不但判完六列，還回頭補上第一批那一格至今唯一的實測值。
      - 後一批結束時必須回掃前一批的列，批次報告固定加一節「對前批列的附帶
        修訂」，沒有就寫「無」。
    〔注意〕此條與 runbooks/backfill.md §2 的取列規則有部分張力，尚未寫入該
    runbook。下次修改該檔時一併寫入，在此之前以 STATUS §5 為準。

§5  「待決 — claim_id 正規表示式的七列既有違規」改列為已決並執行為 schema
    v11（選項 b，登記永久豁免）。條文與理由見 ledger/SCHEMA.md §9 與
    ledger/CHANGELOG.md v11。

§4  下一批改為 5.5 回填。註明本批結束時必須回掃 5.1／5.3——5.5 與那兩節共用
    syndecan-1 與 PBR 兩組數值，2026-07-26-5.5.md 與 2026-07-30-5.3-5.5-pbr.md
    兩份舊報告的判定應一併對照。

§1  schema_version 10 → 11。status 分布與完成率不變（本次未動 claims.csv）。

新增 reports/revisions/2026-08-09-split-6.3.md（待裁示）
    6.3 現有 109 列全部 unverified，是全帳本最大的單一節點。逐列清點後，
    真正的差別不在 claim_type 而在查證來源，可切出兩個與其餘 81 列查證流程
    不同的群：

      6.3.1 藥物開發與法規狀態（20 列）
        查證來源是註冊資料庫與藥證資料庫，不是期刊文獻。runbooks/backfill.md
        步驟 2 的檢索順序對這群是倒過來的。三個後果：tier 六個值沒有一個能
        描述試驗狀態；unsupported 的語意不同（查不到常只代表該試驗從未登記
        或登記在非英語註冊系統，本群至少 5 列涉及中國、俄羅斯、伊朗）；
        以及這群會過期——「處於 Phase II」是帶時間戳的狀態，做完仍需定期重驗，
        而目前 schema 沒有任何欄位記錄這件事。最後一點是本提案最實質的理由：
        把會過期的宣稱與不會過期的混在同一節點，將來的監測迴圈無法只挑該重跑
        的那些。

      6.3.2 抑制劑的化學身分與分類（8 列）
        結構、代號對應、類別歸屬，一次查定終身且與療效無關，但它決定其他列
        該不該併為同一條（SCHEMA §2 的「同一條」認定）。實際上是其他列的前置
        依賴，應最先回填。

    執行成本只是 28 列的 section 欄——依 SCHEMA §3 規則 3，claim_id 前綴不跟著
    改，不動 statement、status 或任何 evidence。提案另附兩件需一併裁示的事：
    6.3.1 的判定準則要不要在 runbook 另寫一節；證據等級那 7 列要不要第三個節點。

    時機：建議在 heparanase.md §7 抽取之前執行。§7 只會往 6.3 加同一種量綱，
    等它抽完再拆，分母更大而混雜結構不變。
